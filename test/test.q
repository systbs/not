var {a, b} = object;

export class {T} Int
{
  func constructor (value:String)
  {
   
  }

  func >> (value:Int) 
  {
    
  }
}


func {T} Int2(args:Int)
{
  var {a, b} = (1 != 2) > (3 == 1);
  return teacher{Int{Int{Int{T}}}}(10);
}