var {a, b} = object;

export class {T} Int
{
  func constructor (value:String)
  {
   
  }

  func >> (value:Int) 
  {
    
  }
}


func {T} Int2(args:Int)
{
  var {T, b} = object;
  return teacher{T}(10);
}